[pytest]
testpaths = tests
pythonpath = .
norecursedirs = .git .venv artifacts data logs uploads
markers =
    unit: isolated deterministic component tests
    regression: offline corpus and behavioral regression tests
    integration: multi-component or CLI integration tests
